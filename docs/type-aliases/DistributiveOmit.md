[Maptoolkit JS](../README.md) / DistributiveOmit

# DistributiveOmit\<T, K\>

> **DistributiveOmit**\<`T`, `K`\> = `T` *extends* `unknown` ? `Omit`\<`T`, `K`\> : `never`

A helper for type to omit a property from a type

## Type Parameters

| Type Parameter |
| ------ |
| `T` |
| `K` *extends* [`DistributiveKeys`](DistributiveKeys.md)\<`T`\> |
