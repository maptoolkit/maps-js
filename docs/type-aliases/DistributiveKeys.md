[Maptoolkit JS](../README.md) / DistributiveKeys

# DistributiveKeys\<T\>

> **DistributiveKeys**\<`T`\> = `T` *extends* `T` ? keyof `T` : `never`

A helper for type to omit a property from a type

## Type Parameters

| Type Parameter |
| ------ |
| `T` |
