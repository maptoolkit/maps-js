[Maptoolkit JS](../README.md) / GlyphMetrics

# GlyphMetrics

> **GlyphMetrics** = `object`

Some metices related to a glyph

## Properties

### isDoubleResolution?

> `optional` **isDoubleResolution**: `boolean`

isDoubleResolution = true for 48px textures
