[Maptoolkit JS](../README.md) / Listener

# Listener()

> **Listener** = (`a`: `any`) => `any`

A listener method used as a callback to events

## Parameters

| Parameter | Type |
| ------ | ------ |
| `a` | `any` |

## Returns

`any`
